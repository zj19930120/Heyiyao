# GitHub 部署完整指南

## 📋 前置准备

### 1. 安装 Git
如果你的系统还没有安装Git,请按以下步骤安装:

**Windows系统:**
1. 访问 Git 官网: https://git-scm.com/download/win
2. 下载并安装 Git for Windows
3. 安装时保持默认选项即可
4. 安装完成后,重启命令行窗口

**验证安装:**
```bash
git --version
```

### 2. 注册 GitHub 账号
1. 访问 https://github.com
2. 点击 "Sign up" 注册账号
3. 验证邮箱

### 3. 配置 Git 用户信息
```bash
git config --global user.name "你的名字"
git config --global user.email "你的邮箱@example.com"
```

## 🚀 部署步骤

### 步骤 1: 初始化本地 Git 仓库

在项目目录下打开命令行(PowerShell或CMD),执行:

```bash
# 初始化Git仓库
git init

# 查看当前状态
git status
```

### 步骤 2: 添加文件到暂存区

```bash
# 添加所有文件
git add .

# 查看暂存状态
git status
```

### 步骤 3: 提交代码

```bash
# 提交代码
git commit -m "初始提交: 核医疗产业综合信息平台"
```

### 步骤 4: 在 GitHub 创建远程仓库

1. 登录 GitHub (https://github.com)
2. 点击右上角的 "+" 号,选择 "New repository"
3. 填写仓库信息:
   - **Repository name**: `nuclear-medicine-platform` (或你喜欢的名字)
   - **Description**: `核医疗产业综合信息平台`
   - **Public/Private**: 选择 Public(公开) 或 Private(私有)
   - **不要勾选** "Initialize this repository with a README"
4. 点击 "Create repository"

### 步骤 5: 连接远程仓库

复制GitHub上显示的仓库URL,然后执行:

```bash
# 添加远程仓库 (替换成你的仓库地址)
git remote add origin https://github.com/你的用户名/你的仓库名.git

# 验证远程仓库
git remote -v
```

### 步骤 6: 推送代码到 GitHub

```bash
# 推送代码到main分支
git branch -M main
git push -u origin main
```

**如果遇到认证问题:**
- GitHub 现在使用 Personal Access Token (PAT) 进行认证
- 需要在 GitHub 设置中生成 Token: Settings → Developer settings → Personal access tokens → Tokens (classic) → Generate new token
- 推送时使用 Token 作为密码

### 步骤 7: 启用 GitHub Pages (可选)

如果想让网站在线访问:

1. 进入你的 GitHub 仓库页面
2. 点击 "Settings" (设置)
3. 在左侧菜单找到 "Pages"
4. 在 "Source" 下选择 "main" 分支
5. 点击 "Save"
6. 等待几分钟后,你的网站将在 `https://你的用户名.github.io/你的仓库名/` 访问

**访问页面:**
- 主页: `https://你的用户名.github.io/你的仓库名/index.html`
- 元素周期表: `https://你的用户名.github.io/你的仓库名/yuansuzhouqibiao.html`
- 数据大屏: `https://你的用户名.github.io/你的仓库名/dashboard.html`

## 📝 后续更新代码

当你修改了代码后,使用以下命令更新到GitHub:

```bash
# 查看修改状态
git status

# 添加修改的文件
git add .

# 提交修改
git commit -m "描述你的修改内容"

# 推送到GitHub
git push
```

## ⚠️ 常见问题

### 问题1: git命令不识别
**解决**: 需要先安装Git,参考上面的"安装Git"部分

### 问题2: 推送时要求输入用户名密码
**解决**: 使用Personal Access Token代替密码

### 问题3: 文件太大无法推送
**解决**: 检查.gitignore文件,确保大文件(如zip)被排除

### 问题4: GitHub Pages 显示404
**解决**: 
- 确保已正确配置GitHub Pages
- 检查分支名称是否正确
- 等待几分钟让GitHub处理

## 🎯 快速命令参考

```bash
# 一键部署(在安装Git并配置好GitHub仓库后)
git init
git add .
git commit -m "初始提交: 核医疗产业综合信息平台"
git branch -M main
git remote add origin https://github.com/你的用户名/你的仓库名.git
git push -u origin main
```

## 📞 需要帮助?

如果遇到问题:
1. 查看错误信息
2. 搜索 GitHub 官方文档
3. 在项目中提交 Issue

---

**祝你部署顺利! 🎉**

